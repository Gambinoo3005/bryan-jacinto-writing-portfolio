---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout 
  title="404 - Page Not Found | Bryan Jacinto"
  description="The page you're looking for doesn't exist."
>
  <section class="w-full min-h-[70vh] flex items-center justify-center" style="background-color: var(--section-bg);">
    <div class="mx-auto max-w-2xl px-5 sm:px-6 text-center">
      <!-- 404 Number with gradient -->
      <div class="mb-6">
        <h1 class="text-[120px] sm:text-[160px] font-black leading-none bg-gradient-to-br from-[var(--color-text)] to-[var(--color-muted)] bg-clip-text text-transparent">
          404
        </h1>
      </div>
      
      <!-- Error message -->
      <h2 class="mb-4">Page Not Found</h2>
      <p class="text-[var(--color-muted)] mb-8 text-base sm:text-lg max-w-md mx-auto">
        The page you're looking for doesn't exist or has been moved. Let's get you back on track.
      </p>
      
      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-3 sm:gap-4">
        <a
          href="/"
          class="btn-primary inline-flex h-11 sm:h-12 items-center justify-center rounded-full px-6 sm:px-8 text-sm sm:text-base font-semibold transition-all duration-200 w-full sm:w-auto"
        >
          Go Home
        </a>
        <a
          href="/work"
          class="snake-border-button group inline-flex h-11 sm:h-12 items-center justify-center rounded-full px-6 sm:px-8 text-sm sm:text-base font-semibold focus:outline-none w-full sm:w-auto"
          aria-label="View my work"
        >
          <span>View My Work</span>
        </a>
      </div>
      
      <!-- Helpful links -->
      <div class="mt-12 pt-8 border-t border-[var(--color-border)]">
        <p class="text-sm text-[var(--color-muted)] mb-3">You might be looking for:</p>
        <nav class="flex flex-wrap items-center justify-center gap-4">
          <a href="/about" class="text-sm hover:text-[var(--color-link)] transition-colors">About</a>
          <span class="text-[var(--color-muted)]">•</span>
          <a href="/articles" class="text-sm hover:text-[var(--color-link)] transition-colors">Articles</a>
          <span class="text-[var(--color-muted)]">•</span>
          <a href="/contact" class="text-sm hover:text-[var(--color-link)] transition-colors">Contact</a>
        </nav>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Ensure proper background gradient for 404 number */
  @supports (background-clip: text) or (-webkit-background-clip: text) {
    h1 {
      -webkit-background-clip: text;
      background-clip: text;
    }
  }
</style>

