---
/** Minimal, theme-aware background: base + vignette + sheen */
/** Matches classes defined in your current global.css */
---
<div aria-hidden="true" class="fixed inset-0 z-0 pointer-events-none">
  <!-- Base vertical gradient -->
  <div class="absolute inset-0 bg-base"></div>

  <!-- Soft edge vignette -->
  <div class="absolute inset-0 bg-vignette"></div>

  <!-- Gentle diagonal sheen -->
  <div class="absolute inset-0 bg-sheen"></div>
</div>
